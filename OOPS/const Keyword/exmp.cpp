#include<iostream>
using namespace std;

int main() {

    // x is constant
    const int x = 10; 

    // initialisation can be done but we can't re-assign a value  
    // x = 5; 

    // int* p = &x;
    // *p = 10;

    
    cout<<x<<endl;

    return 0;
}